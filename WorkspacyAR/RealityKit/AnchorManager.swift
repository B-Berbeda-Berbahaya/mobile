import RealityKit
import Combine

// Manages AnchorEntity creation/removal in the RealityKit scene.
// Responsibility: attach placed entities to the ARView's scene at a
// given world transform, keep track of currently placed PlacedObject
// instances, and remove them on request. Knows nothing about grid logic.

final class AnchorManager {

    // TODO: private(set) var placedObjects: [PlacedObject]

    // TODO: func placeEntity(_ entity: ModelEntity, at worldTransform: simd_float4x4, in arView: ARView) -> PlacedObject
    // TODO: func removeObject(_ object: PlacedObject)
    // TODO: func removeAll()
}
