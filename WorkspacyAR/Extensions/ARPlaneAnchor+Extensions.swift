import ARKit

// Convenience accessors on ARPlaneAnchor. Responsibility: expose the
// plane's world-space corner points / bounding footprint in a form
// PlaneGridExtrapolator can iterate over directly.

// TODO: extension ARPlaneAnchor {
//     var worldSpaceExtentCorners: [SIMD3<Float>] { get }
// }
